version: "3.9"

services:
  tr_cloud_test:
    build: .
    container_name: tr_cloud_test
    env_file:
      - .env
    ports:
      - "${APP_PORT}:80"
    volumes:
      - .:/var/www/html 
    command: php -S 0.0.0.0:80 -t /var/www/html